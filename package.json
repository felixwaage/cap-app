{
  "name": "cap",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "scripts": {
    "build": "cds build/all --clean",
    "schema": "cds compile srv --to sql > srv/src/main/resources/schema.sql",
    "deploy": "cds deploy"
  },
  "dependencies": {
    "@sap/cds": "3.21.2",
    "hdb": "^0.17.1"
  },
  "cds": {
    "build": {
      "target": "."
    },
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ]
      },
      "API_BUSINESS_PARTNER": {
        "kind": "odata",
        "model": "srv/external/API_BUSINESS_PARTNER (1)"
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "devDependencies": {
    "@sap/hdi-deploy": "3.7.0",
    "sqlite3": "^4.1.1"
  }
}